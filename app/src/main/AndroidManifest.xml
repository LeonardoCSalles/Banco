<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Banco"
        tools:targetApi="31">
        <activity
            android:name=".PagarActivity"
            android:exported="false"
            android:label="@string/title_activity_pagar"
            android:theme="@style/Theme.Banco" />
        <activity
            android:name=".TransfActivity"
            android:exported="false"
            android:label="@string/title_activity_transf"
            android:theme="@style/Theme.Banco" />
        <activity
            android:name=".DeposActivity"
            android:exported="false"
            android:label="@string/title_activity_depos"
            android:theme="@style/Theme.Banco" />
        <activity
            android:name=".PixActivity"
            android:exported="false" />
        <meta-data
            android:name="andoid.suppor.PARENT_ACTIVITY"
            android:value=".MainActivity" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>